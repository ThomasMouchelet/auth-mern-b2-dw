const UserController = {
    update: async (req, res) => {
        const { firstName, lastName, password } = req.body
        let { user } = req

        // find user by id

        // if password
            // hash password

        // update user
    }
}